flask>=3.0
svgwrite>=1.4
numpy>=1.26
# Optional — add as needed:
# scipy>=1.12        # signal processing, convex hull, interpolation
# shapely>=2.0       # polygon / path boolean ops, buffering
# Pillow>=10.0       # image loading for image-based generators
# noise>=1.2.2       # Perlin / simplex noise (C extension, fast)
