# ── macOS junk ──
.DS_Store

# ── Python ──
__pycache__/
*.py[cod]
.venv/
venv/

# ── Node ──
node_modules/

# ── Build / cache artifacts ──
*.tsbuildinfo
studio1-tracker/cache/

# ── Claude Code local state (worktrees, machine-local settings) ──
.claude/
