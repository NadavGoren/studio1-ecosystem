# ── macOS junk ──
.DS_Store

# ── Python ──
__pycache__/
*.py[cod]
.venv/
venv/

# ── Node ──
node_modules/

# ── Build / cache artifacts ──
*.tsbuildinfo
dist/
.next/
studio1-tracker/cache/

# ── Claude Code local state (worktrees, machine-local settings) ──
.claude/

# ── Standalone apps (each backed up in its own GitHub repo) ──
/studio-os/Studio-OS/
/plotter-slicer/plotter-ui/
